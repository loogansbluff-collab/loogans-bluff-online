"use client";

import { Text } from "@react-three/drei";
import type { ThreeEvent } from "@react-three/fiber";
import { Color } from "three";
import type { BuildingData } from "@/data/town";

type Handler = (event: ThreeEvent<PointerEvent>) => void;

type Profile = {
  wall: string;
  trim: string;
  door: string;
  sign: string;
  signColor?: string;
  awning?: string;
  feature?: "police" | "bail" | "medical" | "townhall" | "bank" | "general" | "realty" | "auto" | "feed" | "diner" | "motel";
};

type SidingKind = "police" | "bail" | "medical" | "townhall" | "bank" | "general" | "realty" | "auto" | "feed" | "diner" | "grocery" | "pharmacy" | "firehall" | "post" | "dentist" | "vet" | "funeral" | "laundry" | "bakery" | "coffee" | "pizza" | "burger" | "icecream" | "chinese" | "butcher" | "clothing" | "shoe" | "furniture" | "appliance" | "electronics" | "outdoor" | "autoparts" | "tire" | "carwash" | "usedcar" | "taxi" | "busdepot" | "hotel" | "insurance" | "accountant" | "lawyer" | "newspaper" | "radio" | "jewelry" | "florist" | "photo" | "print" | "buildsupply" | "plumbing" | "electrical" | "garden" | "pet" | "smoke" | "arcade" | "bowling" | "pool" | "theater" | "bingo" | "gym" | "tattoo" | "spa" | "travel" | "storage" | "donuts" | "chicken" | "fish";

const PROFILES: Record<string, Profile> = {
  "LB-COPSHOP-001": { wall: "#5d6875", trim: "#d5dde5", door: "#27323d", sign: "POLICE DEPARTMENT", feature: "police" },
  "LB-JAIL-001": { wall: "#4b4b52", trim: "#c9c9ce", door: "#242428", sign: "BAIL BONDS", signColor: "#facc15", feature: "bail" },
  "LB-MEDICAL-001": { wall: "#cfd8dc", trim: "#f8fafc", door: "#6b8796", sign: "BLUFF MEDICAL CLINIC", signColor: "#dc2626", feature: "medical" },
  "LB-TOWNHALL-001": { wall: "#8b6849", trim: "#e8dccb", door: "#4a2d1f", sign: "LOOGANS BLUFF TOWN HALL", feature: "townhall" },
  "LB-HOME-001": { wall: "#7d6a91", trim: "#e9e1f1", door: "#33263e", sign: "BLUFF BANK & TRUST-ISH", feature: "bank" },
  "LB-HOME-002": { wall: "#557995", trim: "#d9e7ef", door: "#294458", sign: "LOOGANS BLUFF GENERAL STORE", awning: "#c59a63", feature: "general" },
  "LB-DUMPHOUSE-001": { wall: "#6f706c", trim: "#c7c2b8", door: "#493d31", sign: "LOOGANS REALTY & PROPERTY", feature: "realty" },
  "LB-BARN-001": { wall: "#7b2d2d", trim: "#e4d5ca", door: "#3f4246", sign: "BLUFF AUTO & TOW", feature: "auto" },
  "LB-BARN-002": { wall: "#6f3b2f", trim: "#dbc9ad", door: "#4a3528", sign: "LOOGANS FEED & FARM SUPPLY", feature: "feed" },
  "LB-HOME-003": { wall: "#c47f25", trim: "#f8e7c3", door: "#7c4619", sign: "BLUFF DINER", awning: "#b91c1c", feature: "diner" },
  "LB-HOME-004": { wall: "#b65f62", trim: "#f6d6d7", door: "#6b2f33", sign: "LOOGANS MOTEL", signColor: "#fde68a", feature: "motel" },
};

const SIDING_KINDS: Partial<Record<string, SidingKind>> = {
  "LB-COPSHOP-001": "police",
  "LB-JAIL-001": "bail",
  "LB-MEDICAL-001": "medical",
  "LB-TOWNHALL-001": "townhall",
  "LB-HOME-001": "bank",
  "LB-HOME-002": "general",
  "LB-DUMPHOUSE-001": "realty",
  "LB-BARN-001": "auto",
  "LB-BARN-002": "feed",
  "LB-HOME-003": "diner",
  "LB-GROCERY-001": "grocery",
  "LB-PHARMACY-001": "pharmacy",
  "LB-FIREHALL-001": "firehall",
  "LB-POST-001": "post",
  "LB-DENTIST-001": "dentist",
  "LB-VET-001": "vet",
  "LB-FUNERAL-001": "funeral",
  "LB-LAUNDRY-001": "laundry",
  "LB-BAKERY-001": "bakery",
  "LB-COFFEE-001": "coffee",
  "LB-PIZZA-001": "pizza",
  "LB-BURGER-001": "burger",
  "LB-ICECREAM-001": "icecream",
  "LB-CHINESE-001": "chinese",
  "LB-BUTCHER-001": "butcher",
  "LB-CLOTHING-001": "clothing",
  "LB-SHOE-001": "shoe",
  "LB-FURNITURE-001": "furniture",
  "LB-APPLIANCE-001": "appliance",
  "LB-ELECTRONICS-001": "electronics",
  "LB-OUTDOOR-001": "outdoor",
  "LB-AUTOPARTS-001": "autoparts",
  "LB-TIRE-001": "tire",
  "LB-CARWASH-001": "carwash",
  "LB-USEDCAR-001": "usedcar",
  "LB-TAXI-001": "taxi",
  "LB-BUSDEPOT-001": "busdepot",
  "LB-HOTEL-001": "hotel",
  "LB-INSURANCE-001": "insurance",
  "LB-ACCOUNTANT-001": "accountant",
  "LB-LAWYER-001": "lawyer",
  "LB-NEWSPAPER-001": "newspaper",
  "LB-RADIO-001": "radio",
  "LB-JEWELRY-001": "jewelry",
  "LB-FLORIST-001": "florist",
  "LB-PHOTO-001": "photo",
  "LB-PRINT-001": "print",
  "LB-BUILDSUPPLY-001": "buildsupply",
  "LB-PLUMBING-001": "plumbing",
  "LB-ELECTRICAL-001": "electrical",
  "LB-GARDEN-001": "garden",
  "LB-PET-001": "pet",
  "LB-SMOKE-001": "smoke",
  "LB-ARCADE-001": "arcade",
  "LB-BOWLING-001": "bowling",
  "LB-POOL-001": "pool",
  "LB-THEATER-001": "theater",
  "LB-BINGO-001": "bingo",
  "LB-GYM-001": "gym",
  "LB-TATTOO-001": "tattoo",
  "LB-SPA-001": "spa",
  "LB-TRAVEL-001": "travel",
  "LB-STORAGE-001": "storage",
  "LB-DONUTS-001": "donuts",
  "LB-CHICKEN-001": "chicken",
  "LB-FISH-001": "fish",
};

const UNDER_CONSTRUCTION_IDS = new Set([
  "LB-GROCERY-001", "LB-PHARMACY-001", "LB-FIREHALL-001", "LB-POST-001", "LB-DENTIST-001", "LB-VET-001",
  "LB-FUNERAL-001", "LB-LAUNDRY-001", "LB-BAKERY-001", "LB-COFFEE-001", "LB-PIZZA-001", "LB-BURGER-001",
  "LB-ICECREAM-001", "LB-CHINESE-001", "LB-BUTCHER-001", "LB-CLOTHING-001", "LB-SHOE-001", "LB-FURNITURE-001",
  "LB-APPLIANCE-001", "LB-ELECTRONICS-001", "LB-OUTDOOR-001", "LB-AUTOPARTS-001", "LB-TIRE-001", "LB-CARWASH-001",
  "LB-USEDCAR-001", "LB-TAXI-001", "LB-BUSDEPOT-001", "LB-HOTEL-001", "LB-INSURANCE-001", "LB-ACCOUNTANT-001",
  "LB-LAWYER-001", "LB-NEWSPAPER-001", "LB-RADIO-001", "LB-JEWELRY-001", "LB-FLORIST-001", "LB-PHOTO-001",
  "LB-PRINT-001", "LB-BUILDSUPPLY-001", "LB-PLUMBING-001", "LB-ELECTRICAL-001", "LB-GARDEN-001", "LB-PET-001",
  "LB-SMOKE-001", "LB-ARCADE-001", "LB-BOWLING-001", "LB-POOL-001", "LB-THEATER-001", "LB-BINGO-001",
  "LB-GYM-001", "LB-TATTOO-001", "LB-SPA-001", "LB-TRAVEL-001", "LB-STORAGE-001", "LB-DONUTS-001",
  "LB-CHICKEN-001", "LB-FISH-001",
]);

function rowFacesNorth(z: number) {
  if (z < 0) {
    const phase = ((-z - 12) % 32 + 32) % 32;
    return phase < 8 || phase > 24;
  }
  const rowIndex = Math.round((32 - z) / 16);
  return Math.abs(rowIndex) % 2 === 1;
}

function darken(color: string, amount = 0.62) {
  return new Color(color).multiplyScalar(amount).getStyle();
}

function SidingStrip({ position, size, color, rotation }: { position: [number, number, number]; size: [number, number, number]; color: string; rotation?: [number, number, number] }) {
  const front = <mesh position={position} rotation={rotation} raycast={() => null}><boxGeometry args={size} /><meshStandardMaterial color={color} /></mesh>;
  const isFrontStrip = position[2] > 0 && Math.abs(size[2] - 0.02) < 0.000001;
  if (!isFrontStrip) return front;

  const backPosition: [number, number, number] = [position[0], position[1], -position[2]];
  return <>{front}<mesh position={backPosition} rotation={rotation} raycast={() => null}><boxGeometry args={size} /><meshStandardMaterial color={color} /></mesh></>;
}

function BusinessSiding({ width, height, depth, wall, kind }: { width: number; height: number; depth: number; wall: string; kind: SidingKind }) {
  const frontZ = depth / 2 + 0.018;
  const leftX = -width / 2 - 0.018;
  const rightX = width / 2 + 0.018;

  if (kind === "police") { const seam = "#3f4852"; const horizontalYs = [0.85, 1.85, 2.85].filter((y) => y < height - 0.25); const frontXs = [-width * 0.27, width * 0.27]; return <group>{horizontalYs.map((y) => <SidingStrip key={`ph-${y}`} position={[0, y, frontZ]} size={[width, 0.075, 0.02]} color={seam} />)}{frontXs.map((x) => <SidingStrip key={`pv-${x}`} position={[x, height / 2, frontZ]} size={[0.075, height, 0.02]} color={seam} />)}{horizontalYs.flatMap((y) => [<SidingStrip key={`pl-${y}`} position={[leftX, y, 0]} size={[0.02, 0.075, depth]} color={seam} />, <SidingStrip key={`pr-${y}`} position={[rightX, y, 0]} size={[0.02, 0.075, depth]} color={seam} />])}</group>; }
  if (kind === "bail") { const seam = "#2f3036"; const frontXs = Array.from({ length: 12 }, (_, i) => -width / 2 + 0.25 + i * ((width - 0.5) / 11)); const sideZs = Array.from({ length: 10 }, (_, i) => -depth / 2 + 0.25 + i * ((depth - 0.5) / 9)); return <group>{frontXs.map((x) => <SidingStrip key={`bf-${x}`} position={[x, height / 2, frontZ]} size={[0.035, height, 0.02]} color={seam} />)}{sideZs.flatMap((z) => [<SidingStrip key={`bl-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.035]} color={seam} />, <SidingStrip key={`br-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.035]} color={seam} />])}</group>; }
  if (kind === "medical") { const seam = "#9aa7ad"; const horizontalYs = [1.05, 2.15, 3.25].filter((y) => y < height - 0.25); const frontXs = [-width * 0.22, width * 0.22]; const sideZs = [-depth * 0.22, depth * 0.22]; return <group>{horizontalYs.map((y) => <SidingStrip key={`mh-${y}`} position={[0, y, frontZ]} size={[width, 0.055, 0.02]} color={seam} />)}{frontXs.map((x) => <SidingStrip key={`mv-${x}`} position={[x, height / 2, frontZ]} size={[0.055, height, 0.02]} color={seam} />)}{horizontalYs.flatMap((y) => [<SidingStrip key={`mlh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.055, depth]} color={seam} />, <SidingStrip key={`mrh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.055, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`mlv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.055]} color={seam} />, <SidingStrip key={`mrv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.055]} color={seam} />])}</group>; }
  if (kind === "townhall") { const seam = "#5f4632"; const rows = [0.72, 1.42, 2.12, 2.82, 3.52, 4.22].filter((y) => y < height - 0.2); return <group>{rows.map((y, index) => <group key={`th-${y}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.08, 0.02]} color={seam} />{[-width * 0.28, width * 0.28].map((x) => <SidingStrip key={`tv-${index}-${x}`} position={[x + (index % 2 ? width * 0.14 : 0), y - 0.35, frontZ]} size={[0.07, 0.62, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.08, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.08, depth]} color={seam} /></group>)}</group>; }
  if (kind === "bank") { const seam = "#554864"; const rows = [0.78, 1.62, 2.46].filter((y) => y < height - 0.25); return <group>{rows.map((y) => <group key={`bk-${y}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.065, 0.02]} color={seam} /><SidingStrip position={[leftX, y, 0]} size={[0.02, 0.065, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.065, depth]} color={seam} /></group>)}</group>; }
  if (kind === "general") { const seam = "#365468"; const frontXs = Array.from({ length: 8 }, (_, i) => -width / 2 + 0.3 + i * ((width - 0.6) / 7)); const sideZs = Array.from({ length: 7 }, (_, i) => -depth / 2 + 0.3 + i * ((depth - 0.6) / 6)); return <group>{frontXs.map((x) => <SidingStrip key={`gf-${x}`} position={[x, height / 2, frontZ]} size={[0.045, height, 0.02]} color={seam} />)}{sideZs.flatMap((z) => [<SidingStrip key={`gl-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.045]} color={seam} />, <SidingStrip key={`gr-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.045]} color={seam} />])}</group>; }

  const seam = darken(wall);

  if (kind === "realty") { const rows = [0.55, 1.35, 2.2, 3.05].filter((y) => y < height - 0.18); return <group>{rows.map((y, index) => <group key={`ry-${y}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.055 + (index % 2) * 0.015, 0.02]} color={seam} /><SidingStrip position={[index % 2 ? width * 0.18 : -width * 0.14, y - 0.38, frontZ]} size={[0.05, 0.7, 0.02]} color={seam} /><SidingStrip position={[leftX, y, 0]} size={[0.02, 0.055, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.055, depth]} color={seam} /></group>)}</group>; }
  if (kind === "auto") { const frontXs = Array.from({ length: 14 }, (_, i) => -width / 2 + 0.18 + i * ((width - 0.36) / 13)); const sideZs = Array.from({ length: 12 }, (_, i) => -depth / 2 + 0.2 + i * ((depth - 0.4) / 11)); return <group>{frontXs.map((x, i) => <SidingStrip key={`af-${x}`} position={[x, height / 2, frontZ]} size={[i % 2 ? 0.025 : 0.045, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`al-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, i % 2 ? 0.025 : 0.045]} color={seam} />, <SidingStrip key={`ar-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, i % 2 ? 0.025 : 0.045]} color={seam} />])}</group>; }
  if (kind === "feed") { const frontXs = Array.from({ length: 6 }, (_, i) => -width / 2 + 0.45 + i * ((width - 0.9) / 5)); const sideZs = Array.from({ length: 7 }, (_, i) => -depth / 2 + 0.45 + i * ((depth - 0.9) / 6)); return <group>{frontXs.map((x) => <SidingStrip key={`ff-${x}`} position={[x, height / 2, frontZ]} size={[0.075, height, 0.02]} color={seam} />)}{sideZs.flatMap((z) => [<SidingStrip key={`fl-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.075]} color={seam} />, <SidingStrip key={`fr-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.075]} color={seam} />])}</group>; }
  if (kind === "diner") { const rows = [0.9, 1.85, 2.8].filter((y) => y < height - 0.2); const cols = [-width * 0.25, width * 0.25]; return <group>{rows.map((y) => <SidingStrip key={`dh-${y}`} position={[0, y, frontZ]} size={[width, 0.045, 0.02]} color={seam} />)}{cols.map((x) => <SidingStrip key={`dv-${x}`} position={[x, height / 2, frontZ]} size={[0.045, height, 0.02]} color={seam} />)}{rows.flatMap((y) => [<SidingStrip key={`dl-${y}`} position={[leftX, y, 0]} size={[0.02, 0.045, depth]} color={seam} />, <SidingStrip key={`dr-${y}`} position={[rightX, y, 0]} size={[0.02, 0.045, depth]} color={seam} />])}</group>; }
  if (kind === "grocery") { const rows = Array.from({ length: Math.max(3, Math.floor(height / 0.55)) }, (_, i) => 0.45 + i * 0.55).filter((y) => y < height - 0.18); return <group>{rows.map((y, row) => <group key={`gh-${y}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.045, 0.02]} color={seam} />{Array.from({ length: 4 }, (_, i) => -width / 2 + (i + 1) * (width / 5) + (row % 2 ? width / 10 : 0)).filter((x) => x < width / 2 - 0.12).map((x) => <SidingStrip key={`gv-${row}-${x}`} position={[x, y - 0.275, frontZ]} size={[0.04, 0.51, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.045, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.045, depth]} color={seam} /></group>)}</group>; }
  if (kind === "pharmacy") { const tile = 0.48; const horizontalYs = Array.from({ length: Math.max(4, Math.floor(height / tile)) }, (_, i) => 0.42 + i * tile).filter((y) => y < height - 0.15); const frontXs = Array.from({ length: 7 }, (_, i) => -width / 2 + 0.35 + i * ((width - 0.7) / 6)); const sideZs = Array.from({ length: 7 }, (_, i) => -depth / 2 + 0.35 + i * ((depth - 0.7) / 6)); return <group>{horizontalYs.map((y) => <SidingStrip key={`pxh-${y}`} position={[0, y, frontZ]} size={[width, 0.035, 0.02]} color={seam} />)}{frontXs.map((x) => <SidingStrip key={`pxv-${x}`} position={[x, height / 2, frontZ]} size={[0.035, height, 0.02]} color={seam} />)}{horizontalYs.flatMap((y) => [<SidingStrip key={`pxlh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.035, depth]} color={seam} />, <SidingStrip key={`pxrh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.035, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`pxlv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.035]} color={seam} />, <SidingStrip key={`pxrv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.035]} color={seam} />])}</group>; }
  if (kind === "firehall") { const rows = Array.from({ length: Math.max(4, Math.floor(height / 0.72)) }, (_, i) => 0.5 + i * 0.72).filter((y) => y < height - 0.16); return <group>{rows.map((y) => <group key={`fh-${y}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.07, 0.02]} color={seam} /><SidingStrip position={[leftX, y, 0]} size={[0.02, 0.07, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.07, depth]} color={seam} /></group>)}</group>; }
  if (kind === "post") { const rows = Array.from({ length: Math.max(4, Math.floor(height / 0.72)) }, (_, i) => 0.58 + i * 0.72).filter((y) => y < height - 0.18); return <group>{rows.map((y, row) => <group key={`po-${y}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.06, 0.02]} color={seam} />{[-0.34, 0.04, 0.38].map((ratio, i) => <SidingStrip key={`pov-${row}-${i}`} position={[width * ratio + (row % 2 ? width * 0.08 : 0), y - 0.34, frontZ]} size={[0.055, 0.62, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.06, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.06, depth]} color={seam} /></group>)}</group>; }
  if (kind === "dentist") { const rows = Array.from({ length: 5 }, (_, i) => 0.52 + i * 0.6).filter((y) => y < height - 0.2); const frontXs = Array.from({ length: 6 }, (_, i) => -width / 2 + 0.42 + i * ((width - 0.84) / 5)); const sideZs = Array.from({ length: 7 }, (_, i) => -depth / 2 + 0.42 + i * ((depth - 0.84) / 6)); return <group>{rows.flatMap((y, row) => frontXs.map((x, i) => <SidingStrip key={`df-${row}-${i}`} position={[x, y, frontZ]} rotation={[0, 0, (row + i) % 2 ? 0.32 : -0.32]} size={[0.026, 0.46, 0.02]} color={seam} />))}{rows.flatMap((y, row) => sideZs.flatMap((z, i) => [<SidingStrip key={`dl-${row}-${i}`} position={[leftX, y, z]} rotation={[(row + i) % 2 ? 0.32 : -0.32, 0, 0]} size={[0.02, 0.46, 0.026]} color={seam} />, <SidingStrip key={`dr-${row}-${i}`} position={[rightX, y, z]} rotation={[(row + i) % 2 ? -0.32 : 0.32, 0, 0]} size={[0.02, 0.46, 0.026]} color={seam} />]))}</group>; }
  if (kind === "vet") { const rows = Array.from({ length: Math.max(6, Math.floor(height / 0.38)) }, (_, i) => 0.3 + i * 0.38).filter((y) => y < height - 0.12); return <group>{rows.map((y, i) => <group key={`vh-${i}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.03, 0.02]} color={seam} /><SidingStrip position={[leftX, y, 0]} size={[0.02, 0.03, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.03, depth]} color={seam} /></group>)}</group>; }
  if (kind === "funeral") { const frontXs = Array.from({ length: 5 }, (_, i) => -width / 2 + 0.5 + i * ((width - 1.0) / 4)); const sideZs = Array.from({ length: 5 }, (_, i) => -depth / 2 + 0.5 + i * ((depth - 1.0) / 4)); return <group>{frontXs.map((x, i) => <SidingStrip key={`fu-f-${i}`} position={[x, height / 2, frontZ]} size={[0.065, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`fu-l-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.065]} color={seam} />, <SidingStrip key={`fu-r-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.065]} color={seam} />])}</group>; }
  if (kind === "bakery") { const rows = Array.from({ length: Math.max(4, Math.floor(height / 0.56)) }, (_, i) => 0.46 + i * 0.56).filter((y) => y < height - 0.16); return <group>{rows.map((y, row) => <group key={`ba-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.035, 0.02]} color={seam} />{Array.from({ length: 7 }, (_, i) => -width / 2 + 0.3 + i * ((width - 0.6) / 6) + (row % 2 ? 0.18 : 0)).filter((x) => x < width / 2 - 0.12).map((x, i) => <SidingStrip key={`baf-${row}-${i}`} position={[x, y - 0.2, frontZ]} size={[0.03, 0.3, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.035, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.035, depth]} color={seam} /></group>)}</group>; }
  if (kind === "coffee") { const frontXs = Array.from({ length: 13 }, (_, i) => -width / 2 + 0.2 + i * ((width - 0.4) / 12)); const sideZs = Array.from({ length: 11 }, (_, i) => -depth / 2 + 0.2 + i * ((depth - 0.4) / 10)); return <group>{frontXs.map((x, i) => <SidingStrip key={`cof-${i}`} position={[x, height / 2, frontZ]} size={[i % 2 ? 0.022 : 0.036, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`col-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, i % 2 ? 0.022 : 0.036]} color={seam} />, <SidingStrip key={`cor-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, i % 2 ? 0.022 : 0.036]} color={seam} />])}</group>; }
  if (kind === "pizza") { const ys = Array.from({ length: 5 }, (_, i) => 0.5 + i * 0.64).filter((y) => y < height - 0.18); const xs = Array.from({ length: 6 }, (_, i) => -width / 2 + 0.4 + i * ((width - 0.8) / 5)); const zs = Array.from({ length: 6 }, (_, i) => -depth / 2 + 0.4 + i * ((depth - 0.8) / 5)); return <group>{ys.flatMap((y, row) => xs.flatMap((x, i) => [<SidingStrip key={`pzf-a-${row}-${i}`} position={[x, y, frontZ]} rotation={[0, 0, 0.58]} size={[0.028, 0.52, 0.02]} color={seam} />, <SidingStrip key={`pzf-b-${row}-${i}`} position={[x, y, frontZ]} rotation={[0, 0, -0.58]} size={[0.028, 0.52, 0.02]} color={seam} />]))}{ys.flatMap((y, row) => zs.flatMap((z, i) => [<SidingStrip key={`pzl-a-${row}-${i}`} position={[leftX, y, z]} rotation={[0.58, 0, 0]} size={[0.02, 0.52, 0.028]} color={seam} />, <SidingStrip key={`pzl-b-${row}-${i}`} position={[leftX, y, z]} rotation={[-0.58, 0, 0]} size={[0.02, 0.52, 0.028]} color={seam} />, <SidingStrip key={`pzr-a-${row}-${i}`} position={[rightX, y, z]} rotation={[0.58, 0, 0]} size={[0.02, 0.52, 0.028]} color={seam} />, <SidingStrip key={`pzr-b-${row}-${i}`} position={[rightX, y, z]} rotation={[-0.58, 0, 0]} size={[0.02, 0.52, 0.028]} color={seam} />]))}</group>; }
  if (kind === "burger") { const rowYs = Array.from({ length: 4 }, (_, i) => 0.68 + i * 0.78).filter((y) => y < height - 0.18); const colXs = [-width * 0.3, 0, width * 0.3]; const sideZs = [-depth * 0.3, 0, depth * 0.3]; return <group>{rowYs.map((y) => <SidingStrip key={`buh-${y}`} position={[0, y, frontZ]} size={[width, 0.06, 0.02]} color={seam} />)}{colXs.map((x) => <SidingStrip key={`buv-${x}`} position={[x, height / 2, frontZ]} size={[0.06, height, 0.02]} color={seam} />)}{rowYs.flatMap((y) => [<SidingStrip key={`bulh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.06, depth]} color={seam} />, <SidingStrip key={`burh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.06, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`bulv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.06]} color={seam} />, <SidingStrip key={`burv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.06]} color={seam} />])}</group>; }
  if (kind === "icecream") { const rows = Array.from({ length: Math.max(4, Math.floor(height / 0.5)) }, (_, i) => 0.42 + i * 0.5).filter((y) => y < height - 0.15); return <group>{rows.map((y, row) => <group key={`ic-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.03, 0.02]} color={seam} />{Array.from({ length: 6 }, (_, i) => -width / 2 + 0.34 + i * ((width - 0.68) / 5) + (row % 2 ? 0.22 : 0)).filter((x) => x < width / 2 - 0.1).map((x, i) => <SidingStrip key={`icf-${row}-${i}`} position={[x, y - 0.16, frontZ]} size={[0.035, 0.24, 0.02]} color={seam} />)}{Array.from({ length: 6 }, (_, i) => -depth / 2 + 0.34 + i * ((depth - 0.68) / 5) + (row % 2 ? 0.22 : 0)).filter((z) => z < depth / 2 - 0.1).flatMap((z, i) => [<SidingStrip key={`icl-${row}-${i}`} position={[leftX, y - 0.16, z]} size={[0.02, 0.24, 0.035]} color={seam} />, <SidingStrip key={`icr-${row}-${i}`} position={[rightX, y - 0.16, z]} size={[0.02, 0.24, 0.035]} color={seam} />])}</group>)}</group>; }
  if (kind === "chinese") { const rowYs = [0.78, 1.62, 2.46, 3.3].filter((y) => y < height - 0.18); const colXs = [-width * 0.32, 0, width * 0.32]; const sideZs = [-depth * 0.32, 0, depth * 0.32]; return <group>{rowYs.map((y) => <SidingStrip key={`chh-${y}`} position={[0, y, frontZ]} size={[width, 0.085, 0.02]} color={seam} />)}{colXs.map((x) => <SidingStrip key={`chv-${x}`} position={[x, height / 2, frontZ]} size={[0.085, height, 0.02]} color={seam} />)}{rowYs.flatMap((y) => [<SidingStrip key={`chlh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.085, depth]} color={seam} />, <SidingStrip key={`chrh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.085, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`chlv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.085]} color={seam} />, <SidingStrip key={`chrv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.085]} color={seam} />])}</group>; }
  if (kind === "butcher") { const frontXs = Array.from({ length: 7 }, (_, i) => -width / 2 + 0.42 + i * ((width - 0.84) / 6)); const sideZs = Array.from({ length: 7 }, (_, i) => -depth / 2 + 0.42 + i * ((depth - 0.84) / 6)); return <group>{frontXs.map((x, i) => <SidingStrip key={`btf-${i}`} position={[x, height / 2, frontZ]} size={[i % 2 ? 0.07 : 0.11, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`btl-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, i % 2 ? 0.07 : 0.11]} color={seam} />, <SidingStrip key={`btr-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, i % 2 ? 0.07 : 0.11]} color={seam} />])}</group>; }
  if (kind === "clothing") { const rows = Array.from({ length: 6 }, (_, i) => 0.48 + i * 0.56).filter((y) => y < height - 0.16); const xs = Array.from({ length: 6 }, (_, i) => -width / 2 + 0.38 + i * ((width - 0.76) / 5)); const zs = Array.from({ length: 6 }, (_, i) => -depth / 2 + 0.38 + i * ((depth - 0.76) / 5)); return <group>{rows.flatMap((y, row) => xs.map((x, i) => <SidingStrip key={`clf-${row}-${i}`} position={[x, y, frontZ]} rotation={[0, 0, (row + i) % 2 ? 0.72 : -0.72]} size={[0.032, 0.5, 0.02]} color={seam} />))}{rows.flatMap((y, row) => zs.flatMap((z, i) => [<SidingStrip key={`cll-${row}-${i}`} position={[leftX, y, z]} rotation={[(row + i) % 2 ? 0.72 : -0.72, 0, 0]} size={[0.02, 0.5, 0.032]} color={seam} />, <SidingStrip key={`clr-${row}-${i}`} position={[rightX, y, z]} rotation={[(row + i) % 2 ? -0.72 : 0.72, 0, 0]} size={[0.02, 0.5, 0.032]} color={seam} />]))}</group>; }
  if (kind === "shoe") { const rows = Array.from({ length: 5 }, (_, i) => 0.58 + i * 0.66).filter((y) => y < height - 0.18); return <group>{rows.map((y, row) => <group key={`sh-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.048, 0.02]} color={seam} />{[-0.28, 0.28].map((ratio, i) => <SidingStrip key={`shf-${row}-${i}`} position={[width * ratio + (row % 2 ? width * 0.16 : 0), y - 0.3, frontZ]} size={[0.048, 0.56, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.048, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.048, depth]} color={seam} /></group>)}</group>; }
  if (kind === "furniture") { const rowYs = [0.9, 2.0, 3.1].filter((y) => y < height - 0.2); const colXs = [-width * 0.3, width * 0.3]; const sideZs = [-depth * 0.3, depth * 0.3]; return <group>{rowYs.map((y) => <SidingStrip key={`frh-${y}`} position={[0, y, frontZ]} size={[width, 0.075, 0.02]} color={seam} />)}{colXs.map((x) => <SidingStrip key={`frv-${x}`} position={[x, height / 2, frontZ]} size={[0.075, height, 0.02]} color={seam} />)}{rowYs.flatMap((y) => [<SidingStrip key={`frlh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.075, depth]} color={seam} />, <SidingStrip key={`frrh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.075, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`frlv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.075]} color={seam} />, <SidingStrip key={`frrv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.075]} color={seam} />])}</group>; }
  if (kind === "appliance") { const frontXs = [-width * 0.24, width * 0.24]; const sideZs = [-depth * 0.24, depth * 0.24]; return <group>{frontXs.map((x, i) => <SidingStrip key={`apf-${i}`} position={[x, height / 2, frontZ]} size={[0.08, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`apl-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.08]} color={seam} />, <SidingStrip key={`apr-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.08]} color={seam} />])}</group>; }
  if (kind === "electronics") { const rows = Array.from({ length: 7 }, (_, i) => 0.36 + i * 0.46).filter((y) => y < height - 0.14); const frontXs = Array.from({ length: 8 }, (_, i) => -width / 2 + 0.3 + i * ((width - 0.6) / 7)); const sideZs = Array.from({ length: 8 }, (_, i) => -depth / 2 + 0.3 + i * ((depth - 0.6) / 7)); return <group>{rows.flatMap((y, row) => frontXs.map((x, i) => <SidingStrip key={`elf-${row}-${i}`} position={[x, y, frontZ]} size={[0.045, 0.045, 0.02]} color={seam} />))}{rows.flatMap((y, row) => sideZs.flatMap((z, i) => [<SidingStrip key={`ell-${row}-${i}`} position={[leftX, y, z]} size={[0.02, 0.045, 0.045]} color={seam} />, <SidingStrip key={`elr-${row}-${i}`} position={[rightX, y, z]} size={[0.02, 0.045, 0.045]} color={seam} />]))}</group>; }
  if (kind === "outdoor") { const rows = Array.from({ length: 5 }, (_, i) => 0.52 + i * 0.68).filter((y) => y < height - 0.16); return <group>{rows.map((y, row) => <group key={`ou-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.05, 0.02]} color={seam} />{Array.from({ length: 5 }, (_, i) => -width / 2 + 0.48 + i * ((width - 0.96) / 4) + (row % 2 ? 0.28 : 0)).filter((x) => x < width / 2 - 0.14).map((x, i) => <SidingStrip key={`ouf-${row}-${i}`} position={[x, y - 0.24, frontZ]} size={[0.05, 0.38, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.05, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.05, depth]} color={seam} /></group>)}</group>; }
  if (kind === "autoparts") { const ys = Array.from({ length: 5 }, (_, i) => 0.5 + i * 0.64).filter((y) => y < height - 0.18); const xs = Array.from({ length: 6 }, (_, i) => -width / 2 + 0.42 + i * ((width - 0.84) / 5)); const zs = Array.from({ length: 6 }, (_, i) => -depth / 2 + 0.42 + i * ((depth - 0.84) / 5)); return <group>{ys.flatMap((y, row) => xs.map((x, i) => <SidingStrip key={`atf-${row}-${i}`} position={[x, y, frontZ]} rotation={[0, 0, (row + i) % 2 ? 0.65 : -0.65]} size={[0.04, 0.56, 0.02]} color={seam} />))}{ys.flatMap((y, row) => zs.flatMap((z, i) => [<SidingStrip key={`atl-${row}-${i}`} position={[leftX, y, z]} rotation={[(row + i) % 2 ? 0.65 : -0.65, 0, 0]} size={[0.02, 0.56, 0.04]} color={seam} />, <SidingStrip key={`atr-${row}-${i}`} position={[rightX, y, z]} rotation={[(row + i) % 2 ? -0.65 : 0.65, 0, 0]} size={[0.02, 0.56, 0.04]} color={seam} />]))}</group>; }
  if (kind === "tire") { const rows = [0.72, 1.62, 2.52, 3.42].filter((y) => y < height - 0.18); return <group>{rows.flatMap((y) => [<SidingStrip key={`tif-${y}`} position={[0, y, frontZ]} size={[width, 0.12, 0.02]} color={seam} />, <SidingStrip key={`til-${y}`} position={[leftX, y, 0]} size={[0.02, 0.12, depth]} color={seam} />, <SidingStrip key={`tir-${y}`} position={[rightX, y, 0]} size={[0.02, 0.12, depth]} color={seam} />])}</group>; }
  if (kind === "carwash") { const frontXs = Array.from({ length: 5 }, (_, i) => -width / 2 + 0.6 + i * ((width - 1.2) / 4)); const sideZs = Array.from({ length: 5 }, (_, i) => -depth / 2 + 0.6 + i * ((depth - 1.2) / 4)); return <group>{frontXs.map((x, i) => <SidingStrip key={`cwf-${i}`} position={[x, height / 2, frontZ]} size={[0.12, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`cwl-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.12]} color={seam} />, <SidingStrip key={`cwr-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.12]} color={seam} />])}</group>; }
  if (kind === "usedcar") { const rowYs = [1.1, 2.25, 3.4].filter((y) => y < height - 0.18); const colXs = [-width * 0.34, 0, width * 0.34]; const sideZs = [-depth * 0.34, 0, depth * 0.34]; return <group>{rowYs.map((y) => <SidingStrip key={`uch-${y}`} position={[0, y, frontZ]} size={[width, 0.09, 0.02]} color={seam} />)}{colXs.map((x) => <SidingStrip key={`ucv-${x}`} position={[x, height / 2, frontZ]} size={[0.09, height, 0.02]} color={seam} />)}{rowYs.flatMap((y) => [<SidingStrip key={`uclh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.09, depth]} color={seam} />, <SidingStrip key={`ucrh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.09, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`uclv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.09]} color={seam} />, <SidingStrip key={`ucrv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.09]} color={seam} />])}</group>; }
  if (kind === "taxi") { const pairs = [-width * 0.28, 0, width * 0.28]; const sidePairs = [-depth * 0.28, 0, depth * 0.28]; return <group>{pairs.flatMap((x, i) => [<SidingStrip key={`txf-a-${i}`} position={[x - 0.055, height / 2, frontZ]} size={[0.025, height, 0.02]} color={seam} />, <SidingStrip key={`txf-b-${i}`} position={[x + 0.055, height / 2, frontZ]} size={[0.025, height, 0.02]} color={seam} />])}{sidePairs.flatMap((z, i) => [<SidingStrip key={`txl-a-${i}`} position={[leftX, height / 2, z - 0.055]} size={[0.02, height, 0.025]} color={seam} />, <SidingStrip key={`txl-b-${i}`} position={[leftX, height / 2, z + 0.055]} size={[0.02, height, 0.025]} color={seam} />, <SidingStrip key={`txr-a-${i}`} position={[rightX, height / 2, z - 0.055]} size={[0.02, height, 0.025]} color={seam} />, <SidingStrip key={`txr-b-${i}`} position={[rightX, height / 2, z + 0.055]} size={[0.02, height, 0.025]} color={seam} />])}</group>; }
  if (kind === "busdepot") { const rowYs = [0.9, 2.0, 3.1].filter((y) => y < height - 0.18); const colXs = [-width * 0.3, 0, width * 0.3]; const sideZs = [-depth * 0.3, 0, depth * 0.3]; return <group>{rowYs.map((y) => <SidingStrip key={`bdh-${y}`} position={[0, y, frontZ]} size={[width, 0.07, 0.02]} color={seam} />)}{colXs.map((x) => <SidingStrip key={`bdv-${x}`} position={[x, height / 2, frontZ]} size={[0.07, height, 0.02]} color={seam} />)}{rowYs.flatMap((y) => [<SidingStrip key={`bdlh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.07, depth]} color={seam} />, <SidingStrip key={`bdrh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.07, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`bdlv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.07]} color={seam} />, <SidingStrip key={`bdrv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.07]} color={seam} />])}</group>; }
  if (kind === "hotel") { const rows = [height * 0.28, height * 0.52, height * 0.76].filter((y) => y < height - 0.18); return <group>{rows.flatMap((y, i) => [<SidingStrip key={`ho-f-${i}`} position={[0, y, frontZ]} size={[width, 0.12, 0.02]} color={seam} />, <SidingStrip key={`ho-l-${i}`} position={[leftX, y, 0]} size={[0.02, 0.12, depth]} color={seam} />, <SidingStrip key={`ho-r-${i}`} position={[rightX, y, 0]} size={[0.02, 0.12, depth]} color={seam} />])}</group>; }
  if (kind === "insurance") { const frontXs = Array.from({ length: 8 }, (_, i) => -width / 2 + 0.32 + i * ((width - 0.64) / 7)); const sideZs = Array.from({ length: 8 }, (_, i) => -depth / 2 + 0.32 + i * ((depth - 0.64) / 7)); const rows = [0.9, 1.8, 2.7].filter((y) => y < height - 0.16); return <group>{frontXs.map((x, i) => <SidingStrip key={`inf-${i}`} position={[x, height / 2, frontZ]} size={[0.05, height, 0.02]} color={seam} />)}{rows.map((y, i) => <SidingStrip key={`inh-${i}`} position={[0, y, frontZ]} size={[width, 0.032, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`inl-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.05]} color={seam} />, <SidingStrip key={`inr-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.05]} color={seam} />])}</group>; }
  if (kind === "accountant") { const rows = Array.from({ length: Math.max(7, Math.floor(height / 0.34)) }, (_, i) => 0.26 + i * 0.34).filter((y) => y < height - 0.12); return <group>{rows.flatMap((y, i) => [<SidingStrip key={`acf-${i}`} position={[0, y, frontZ]} size={[width, i % 3 === 0 ? 0.045 : 0.026, 0.02]} color={seam} />, <SidingStrip key={`acl-${i}`} position={[leftX, y, 0]} size={[0.02, i % 3 === 0 ? 0.045 : 0.026, depth]} color={seam} />, <SidingStrip key={`acr-${i}`} position={[rightX, y, 0]} size={[0.02, i % 3 === 0 ? 0.045 : 0.026, depth]} color={seam} />])}</group>; }
  if (kind === "lawyer") { const rowYs = [0.82, 1.72, 2.62, 3.52].filter((y) => y < height - 0.18); const colXs = [-width * 0.34, 0, width * 0.34]; const sideZs = [-depth * 0.34, 0, depth * 0.34]; return <group>{rowYs.map((y) => <SidingStrip key={`lah-${y}`} position={[0, y, frontZ]} size={[width, 0.11, 0.02]} color={seam} />)}{colXs.map((x) => <SidingStrip key={`lav-${x}`} position={[x, height / 2, frontZ]} size={[0.11, height, 0.02]} color={seam} />)}{rowYs.flatMap((y) => [<SidingStrip key={`lalh-${y}`} position={[leftX, y, 0]} size={[0.02, 0.11, depth]} color={seam} />, <SidingStrip key={`larh-${y}`} position={[rightX, y, 0]} size={[0.02, 0.11, depth]} color={seam} />])}{sideZs.flatMap((z) => [<SidingStrip key={`lalv-${z}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.11]} color={seam} />, <SidingStrip key={`larv-${z}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.11]} color={seam} />])}</group>; }
  if (kind === "newspaper") { const frontXs = [-0.42, -0.18, 0.04, 0.31, 0.47].map((ratio) => width * ratio); const sideZs = [-0.42, -0.18, 0.04, 0.31, 0.47].map((ratio) => depth * ratio); return <group>{frontXs.map((x, i) => <SidingStrip key={`npf-${i}`} position={[x, height / 2, frontZ]} size={[i % 2 ? 0.04 : 0.07, height, 0.02]} color={seam} />)}{sideZs.flatMap((z, i) => [<SidingStrip key={`npl-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, i % 2 ? 0.04 : 0.07]} color={seam} />, <SidingStrip key={`npr-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, i % 2 ? 0.04 : 0.07]} color={seam} />])}</group>; }
  if (kind === "radio") { const sideYs = [0.7, 1.3, 1.9, 2.5, 3.1, 3.7].filter((y) => y < height - 0.14); return <group><SidingStrip position={[0, height / 2, frontZ]} size={[0.095, height, 0.02]} color={seam} />{sideYs.flatMap((y, i) => [<SidingStrip key={`rd-l-${i}`} position={[leftX, y, 0]} size={[0.02, 0.055, depth * 0.48]} color={seam} />, <SidingStrip key={`rd-r-${i}`} position={[rightX, y, 0]} size={[0.02, 0.055, depth * 0.48]} color={seam} />])}</group>; }
  if (kind === "jewelry") { const ys = Array.from({ length: 7 }, (_, i) => 0.38 + i * 0.48).filter((y) => y < height - 0.14); const xs = Array.from({ length: 8 }, (_, i) => -width / 2 + 0.3 + i * ((width - 0.6) / 7)); const zs = Array.from({ length: 8 }, (_, i) => -depth / 2 + 0.3 + i * ((depth - 0.6) / 7)); return <group>{ys.flatMap((y, row) => xs.map((x, i) => <SidingStrip key={`jw-f-${row}-${i}`} position={[x, y, frontZ]} rotation={[0, 0, Math.PI / 4]} size={[0.055, 0.055, 0.02]} color={seam} />))}{ys.flatMap((y, row) => zs.flatMap((z, i) => [<SidingStrip key={`jw-l-${row}-${i}`} position={[leftX, y, z]} rotation={[Math.PI / 4, 0, 0]} size={[0.02, 0.055, 0.055]} color={seam} />, <SidingStrip key={`jw-r-${row}-${i}`} position={[rightX, y, z]} rotation={[Math.PI / 4, 0, 0]} size={[0.02, 0.055, 0.055]} color={seam} />]))}</group>; }
  if (kind === "florist") { const rows = Array.from({ length: 6 }, (_, i) => 0.45 + i * 0.55).filter((y) => y < height - 0.16); const frontXs = Array.from({ length: 7 }, (_, i) => -width / 2 + 0.34 + i * ((width - 0.68) / 6)); const sideZs = Array.from({ length: 7 }, (_, i) => -depth / 2 + 0.34 + i * ((depth - 0.68) / 6)); return <group>{rows.flatMap((y, row) => frontXs.map((x, i) => <SidingStrip key={`fl-f-${row}-${i}`} position={[x + (row % 2 ? 0.16 : 0), y, frontZ]} size={[0.035, 0.32, 0.02]} color={seam} />))}{rows.flatMap((y, row) => sideZs.flatMap((z, i) => [<SidingStrip key={`fl-l-${row}-${i}`} position={[leftX, y, z + (row % 2 ? 0.16 : 0)]} size={[0.02, 0.32, 0.035]} color={seam} />, <SidingStrip key={`fl-r-${row}-${i}`} position={[rightX, y, z + (row % 2 ? 0.16 : 0)]} size={[0.02, 0.32, 0.035]} color={seam} />]))}</group>; }
  if (kind === "photo") { const ys = Array.from({ length: 8 }, (_, i) => 0.35 + i * 0.42).filter((y) => y < height - 0.12); const edgeX = width / 2 - 0.18; const edgeZ = depth / 2 - 0.18; return <group>{ys.flatMap((y, i) => [<SidingStrip key={`ph-f-l-${i}`} position={[-edgeX, y, frontZ]} size={[0.075, 0.075, 0.02]} color={seam} />, <SidingStrip key={`ph-f-r-${i}`} position={[edgeX, y, frontZ]} size={[0.075, 0.075, 0.02]} color={seam} />, <SidingStrip key={`ph-l-a-${i}`} position={[leftX, y, -edgeZ]} size={[0.02, 0.075, 0.075]} color={seam} />, <SidingStrip key={`ph-l-b-${i}`} position={[leftX, y, edgeZ]} size={[0.02, 0.075, 0.075]} color={seam} />, <SidingStrip key={`ph-r-a-${i}`} position={[rightX, y, -edgeZ]} size={[0.02, 0.075, 0.075]} color={seam} />, <SidingStrip key={`ph-r-b-${i}`} position={[rightX, y, edgeZ]} size={[0.02, 0.075, 0.075]} color={seam} />])}</group>; }
  if (kind === "print") { const rows = Array.from({ length: 9 }, (_, i) => 0.28 + i * 0.38).filter((y) => y < height - 0.12); const frontXs = Array.from({ length: 10 }, (_, i) => -width / 2 + 0.25 + i * ((width - 0.5) / 9)); const sideZs = Array.from({ length: 10 }, (_, i) => -depth / 2 + 0.25 + i * ((depth - 0.5) / 9)); return <group>{rows.map((y, i) => <SidingStrip key={`pr-h-${i}`} position={[0, y, frontZ]} size={[width, 0.022, 0.02]} color={seam} />)}{frontXs.map((x, i) => <SidingStrip key={`pr-v-${i}`} position={[x, height / 2, frontZ]} size={[0.022, height, 0.02]} color={seam} />)}{rows.flatMap((y, i) => [<SidingStrip key={`pr-lh-${i}`} position={[leftX, y, 0]} size={[0.02, 0.022, depth]} color={seam} />, <SidingStrip key={`pr-rh-${i}`} position={[rightX, y, 0]} size={[0.02, 0.022, depth]} color={seam} />])}{sideZs.flatMap((z, i) => [<SidingStrip key={`pr-lv-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.022]} color={seam} />, <SidingStrip key={`pr-rv-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.022]} color={seam} />])}</group>; }
  if (kind === "buildsupply") { const rows = Array.from({ length: 6 }, (_, i) => 0.5 + i * 0.62).filter((y) => y < height - 0.16); return <group>{rows.map((y, row) => <group key={`bs-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.09, 0.02]} color={seam} /><SidingStrip position={[row % 2 ? width * 0.24 : -width * 0.24, y - 0.28, frontZ]} size={[0.06, 0.46, 0.02]} color={seam} /><SidingStrip position={[leftX, y, 0]} size={[0.02, 0.09, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.09, depth]} color={seam} /></group>)}</group>; }
  if (kind === "plumbing") { const ys = [0.68, 1.42, 2.16, 2.9].filter((y) => y < height - 0.16); const frontXs = [-width * 0.28, 0, width * 0.28]; const sideZs = [-depth * 0.28, 0, depth * 0.28]; return <group>{ys.flatMap((y, row) => frontXs.map((x, i) => <SidingStrip key={`pl-f-${row}-${i}`} position={[x, y, frontZ]} size={[0.5, 0.1, 0.02]} color={seam} />))}{ys.flatMap((y, row) => sideZs.flatMap((z, i) => [<SidingStrip key={`pl-l-${row}-${i}`} position={[leftX, y, z]} size={[0.02, 0.1, 0.5]} color={seam} />, <SidingStrip key={`pl-r-${row}-${i}`} position={[rightX, y, z]} size={[0.02, 0.1, 0.5]} color={seam} />]))}</group>; }
  if (kind === "electrical") { const ys = [0.65, 1.35, 2.05, 2.75, 3.45].filter((y) => y < height - 0.16); const frontDrops = [-width * 0.26, width * 0.22]; const sideDrops = [-depth * 0.24, depth * 0.24]; return <group>{ys.map((y, i) => <SidingStrip key={`el-h-${i}`} position={[0, y, frontZ]} size={[width, 0.035, 0.02]} color={seam} />)}{frontDrops.map((x, i) => <SidingStrip key={`el-v-${i}`} position={[x, height * 0.5, frontZ]} size={[0.035, height * 0.55, 0.02]} color={seam} />)}{ys.flatMap((y, i) => [<SidingStrip key={`el-lh-${i}`} position={[leftX, y, 0]} size={[0.02, 0.035, depth]} color={seam} />, <SidingStrip key={`el-rh-${i}`} position={[rightX, y, 0]} size={[0.02, 0.035, depth]} color={seam} />])}{sideDrops.flatMap((z, i) => [<SidingStrip key={`el-lv-${i}`} position={[leftX, height * 0.5, z]} size={[0.02, height * 0.55, 0.035]} color={seam} />, <SidingStrip key={`el-rv-${i}`} position={[rightX, height * 0.5, z]} size={[0.02, height * 0.55, 0.035]} color={seam} />])}</group>; }
  if (kind === "garden") { const ys = [0.8, 1.7, 2.6, 3.5].filter((y) => y < height - 0.18); const frontXs = [-width * 0.28, 0, width * 0.28]; const sideZs = [-depth * 0.28, 0, depth * 0.28]; return <group>{ys.map((y, i) => <SidingStrip key={`ga-h-${i}`} position={[0, y, frontZ]} size={[width, 0.04, 0.02]} color={seam} />)}{frontXs.map((x, i) => <SidingStrip key={`ga-v-${i}`} position={[x, height / 2, frontZ]} size={[0.04, height, 0.02]} color={seam} />)}{ys.flatMap((y, i) => [<SidingStrip key={`ga-lh-${i}`} position={[leftX, y, 0]} size={[0.02, 0.04, depth]} color={seam} />, <SidingStrip key={`ga-rh-${i}`} position={[rightX, y, 0]} size={[0.02, 0.04, depth]} color={seam} />])}{sideZs.flatMap((z, i) => [<SidingStrip key={`ga-lv-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.04]} color={seam} />, <SidingStrip key={`ga-rv-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.04]} color={seam} />])}</group>; }
  if (kind === "pet") { const ys = [0.95, 2.0, 3.05].filter((y) => y < height - 0.18); const frontXs = [-width * 0.32, 0, width * 0.32]; const sideZs = [-depth * 0.32, 0, depth * 0.32]; return <group>{ys.map((y, i) => <SidingStrip key={`pt-h-${i}`} position={[0, y, frontZ]} size={[width, 0.085, 0.02]} color={seam} />)}{frontXs.map((x, i) => <SidingStrip key={`pt-v-${i}`} position={[x, height / 2, frontZ]} size={[0.085, height, 0.02]} color={seam} />)}{ys.flatMap((y, i) => [<SidingStrip key={`pt-lh-${i}`} position={[leftX, y, 0]} size={[0.02, 0.085, depth]} color={seam} />, <SidingStrip key={`pt-rh-${i}`} position={[rightX, y, 0]} size={[0.02, 0.085, depth]} color={seam} />])}{sideZs.flatMap((z, i) => [<SidingStrip key={`pt-lv-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.085]} color={seam} />, <SidingStrip key={`pt-rv-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.085]} color={seam} />])}</group>; }
  if (kind === "smoke") { const rows = Array.from({ length: Math.max(7, Math.floor(height / 0.42)) }, (_, i) => 0.34 + i * 0.42).filter((y) => y < height - 0.14); return <group>{rows.map((y, row) => <group key={`sm-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.05, 0.02]} color={seam} />{[-0.34, 0.02, 0.36].map((ratio, i) => <SidingStrip key={`sm-v-${row}-${i}`} position={[width * ratio + (row % 2 ? width * 0.08 : 0), y - 0.2, frontZ]} size={[0.045, 0.36, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.05, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.05, depth]} color={seam} /></group>)}</group>; }
  if (kind === "arcade") { const ys = [0.72, 1.58, 2.44, 3.3].filter((y) => y < height - 0.16); return <group>{ys.flatMap((y, i) => [<SidingStrip key={`ar-f-${i}`} position={[0, y, frontZ]} size={[width, 0.14, 0.02]} color={seam} />, <SidingStrip key={`ar-l-${i}`} position={[leftX, y, 0]} size={[0.02, 0.14, depth]} color={seam} />, <SidingStrip key={`ar-r-${i}`} position={[rightX, y, 0]} size={[0.02, 0.14, depth]} color={seam} />])}</group>; }
  if (kind === "bowling") { const lanes = [0.6, 1.15, 1.7, 2.25, 2.8, 3.35].filter((y) => y < height - 0.14); return <group>{lanes.flatMap((y, i) => [<SidingStrip key={`bw-f-${i}`} position={[0, y, frontZ]} size={[width, 0.032, 0.02]} color={seam} />, <SidingStrip key={`bw-l-${i}`} position={[leftX, y, 0]} size={[0.02, 0.032, depth]} color={seam} />, <SidingStrip key={`bw-r-${i}`} position={[rightX, y, 0]} size={[0.02, 0.032, depth]} color={seam} />])}<SidingStrip position={[-width * 0.34, height / 2, frontZ]} size={[0.095, height, 0.02]} color={seam} /><SidingStrip position={[leftX, height / 2, -depth * 0.34]} size={[0.02, height, 0.095]} color={seam} /><SidingStrip position={[rightX, height / 2, -depth * 0.34]} size={[0.02, height, 0.095]} color={seam} /></group>; }
  if (kind === "pool") { const frontCenters = [-width * 0.28, 0, width * 0.28]; const sideCenters = [-depth * 0.28, 0, depth * 0.28]; const y = height * 0.52; return <group>{frontCenters.flatMap((x, i) => [<SidingStrip key={`po-f-a-${i}`} position={[x - 0.18, y, frontZ]} rotation={[0, 0, 0.72]} size={[0.038, 0.5, 0.02]} color={seam} />, <SidingStrip key={`po-f-b-${i}`} position={[x + 0.18, y, frontZ]} rotation={[0, 0, -0.72]} size={[0.038, 0.5, 0.02]} color={seam} />, <SidingStrip key={`po-f-c-${i}`} position={[x - 0.18, y - 0.36, frontZ]} rotation={[0, 0, -0.72]} size={[0.038, 0.5, 0.02]} color={seam} />, <SidingStrip key={`po-f-d-${i}`} position={[x + 0.18, y - 0.36, frontZ]} rotation={[0, 0, 0.72]} size={[0.038, 0.5, 0.02]} color={seam} />])}{sideCenters.flatMap((z, i) => [<SidingStrip key={`po-l-a-${i}`} position={[leftX, y, z - 0.18]} rotation={[0.72, 0, 0]} size={[0.02, 0.5, 0.038]} color={seam} />, <SidingStrip key={`po-l-b-${i}`} position={[leftX, y, z + 0.18]} rotation={[-0.72, 0, 0]} size={[0.02, 0.5, 0.038]} color={seam} />, <SidingStrip key={`po-r-a-${i}`} position={[rightX, y, z - 0.18]} rotation={[0.72, 0, 0]} size={[0.02, 0.5, 0.038]} color={seam} />, <SidingStrip key={`po-r-b-${i}`} position={[rightX, y, z + 0.18]} rotation={[-0.72, 0, 0]} size={[0.02, 0.5, 0.038]} color={seam} />])}</group>; }
  if (kind === "theater") { const centers = [-width * 0.29, 0, width * 0.29]; const sideCenters = [-depth * 0.29, 0, depth * 0.29]; const panelH = Math.min(height * 0.62, 2.55); const panelW = Math.min(width * 0.2, 0.9); return <group>{centers.flatMap((x, i) => [<SidingStrip key={`th-f-l-${i}`} position={[x - panelW / 2, height * 0.48, frontZ]} size={[0.055, panelH, 0.02]} color={seam} />, <SidingStrip key={`th-f-r-${i}`} position={[x + panelW / 2, height * 0.48, frontZ]} size={[0.055, panelH, 0.02]} color={seam} />, <SidingStrip key={`th-f-t-${i}`} position={[x, height * 0.48 + panelH / 2, frontZ]} size={[panelW, 0.055, 0.02]} color={seam} />, <SidingStrip key={`th-f-b-${i}`} position={[x, height * 0.48 - panelH / 2, frontZ]} size={[panelW, 0.055, 0.02]} color={seam} />])}{sideCenters.flatMap((z, i) => [<SidingStrip key={`th-l-v-${i}`} position={[leftX, height * 0.48, z]} size={[0.02, panelH, 0.055]} color={seam} />, <SidingStrip key={`th-r-v-${i}`} position={[rightX, height * 0.48, z]} size={[0.02, panelH, 0.055]} color={seam} />])}</group>; }
  if (kind === "bingo") { const ys = [1.0, 2.05, 3.1].filter((y) => y < height - 0.18); const frontXs = [-width * 0.25, width * 0.25]; const sideZs = [-depth * 0.25, depth * 0.25]; return <group>{ys.map((y, i) => <SidingStrip key={`bi-h-${i}`} position={[0, y, frontZ]} size={[width, 0.065, 0.02]} color={seam} />)}{frontXs.map((x, i) => <SidingStrip key={`bi-v-${i}`} position={[x, height / 2, frontZ]} size={[0.065, height, 0.02]} color={seam} />)}{ys.flatMap((y, i) => [<SidingStrip key={`bi-lh-${i}`} position={[leftX, y, 0]} size={[0.02, 0.065, depth]} color={seam} />, <SidingStrip key={`bi-rh-${i}`} position={[rightX, y, 0]} size={[0.02, 0.065, depth]} color={seam} />])}{sideZs.flatMap((z, i) => [<SidingStrip key={`bi-lv-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.065]} color={seam} />, <SidingStrip key={`bi-rv-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.065]} color={seam} />])}</group>; }
  if (kind === "gym") { const rows = [0.72, 1.44, 2.16, 2.88, 3.6].filter((y) => y < height - 0.16); return <group>{rows.map((y, row) => <group key={`gy-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.055, 0.02]} color={seam} />{[-0.32, 0.02, 0.34].map((ratio, i) => <SidingStrip key={`gy-v-${row}-${i}`} position={[width * ratio + (row % 2 ? width * 0.11 : 0), y - 0.33, frontZ]} size={[0.05, 0.62, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.055, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.055, depth]} color={seam} /></group>)}</group>; }
  if (kind === "tattoo") { const plates = [[-0.34, 0.72, 0.26, 0.46], [0.05, 0.9, 0.34, 0.58], [0.31, 1.55, 0.22, 0.74], [-0.16, 2.0, 0.42, 0.36], [0.24, 2.7, 0.3, 0.48], [-0.3, 3.18, 0.24, 0.52]]; return <group>{plates.filter(([, y]) => y < height - 0.12).map(([rx, y, w, h], i) => <group key={`ta-f-${i}`}><SidingStrip position={[width * rx - width * w / 2, y, frontZ]} size={[0.045, h, 0.02]} color={seam} /><SidingStrip position={[width * rx + width * w / 2, y, frontZ]} size={[0.045, h, 0.02]} color={seam} /><SidingStrip position={[width * rx, y - h / 2, frontZ]} size={[width * w, 0.045, 0.02]} color={seam} /><SidingStrip position={[width * rx, y + h / 2, frontZ]} size={[width * w, 0.045, 0.02]} color={seam} /></group>)}</group>; }
  if (kind === "spa") { const rows = [height * 0.3, height * 0.52, height * 0.74].filter((y) => y < height - 0.16); return <group>{rows.flatMap((y, i) => [<SidingStrip key={`sp-f-${i}`} position={[0, y, frontZ]} size={[width, 0.16, 0.02]} color={seam} />, <SidingStrip key={`sp-l-${i}`} position={[leftX, y, 0]} size={[0.02, 0.16, depth]} color={seam} />, <SidingStrip key={`sp-r-${i}`} position={[rightX, y, 0]} size={[0.02, 0.16, depth]} color={seam} />])}</group>; }
  if (kind === "travel") { const ys = [height * 0.42, height * 0.54, height * 0.66].filter((y) => y < height - 0.16); return <group>{ys.flatMap((y, i) => [<SidingStrip key={`tr-f-${i}`} position={[0, y, frontZ]} size={[width * (i === 1 ? 0.84 : 0.68), 0.065, 0.02]} color={seam} />, <SidingStrip key={`tr-l-${i}`} position={[leftX, y, 0]} size={[0.02, 0.065, depth * (i === 1 ? 0.84 : 0.68)]} color={seam} />, <SidingStrip key={`tr-r-${i}`} position={[rightX, y, 0]} size={[0.02, 0.065, depth * (i === 1 ? 0.84 : 0.68)]} color={seam} />])}</group>; }
  if (kind === "storage") { const frontXs = Array.from({ length: 15 }, (_, i) => -width / 2 + 0.22 + i * ((width - 0.44) / 14)); return <group>{frontXs.map((x, i) => <SidingStrip key={`st-f-${i}`} position={[x, height / 2, frontZ]} size={[i % 3 === 0 ? 0.04 : 0.022, height, 0.02]} color={seam} />)}</group>; }
  if (kind === "donuts") { const bandY = height * 0.72; const drops = [-0.36, -0.18, 0.02, 0.23, 0.39]; return <group><SidingStrip position={[0, bandY, frontZ]} size={[width, 0.13, 0.02]} color={seam} />{drops.map((ratio, i) => <SidingStrip key={`do-f-${i}`} position={[width * ratio, bandY - 0.22 - (i % 2) * 0.11, frontZ]} size={[0.065, 0.34 + (i % 2) * 0.2, 0.02]} color={seam} />)}<SidingStrip position={[leftX, bandY, 0]} size={[0.02, 0.13, depth]} color={seam} /><SidingStrip position={[rightX, bandY, 0]} size={[0.02, 0.13, depth]} color={seam} /></group>; }
  if (kind === "chicken") { const ys = [1.05, 2.15, 3.25].filter((y) => y < height - 0.18); const frontXs = [-width * 0.36, 0, width * 0.36]; const sideZs = [-depth * 0.36, 0, depth * 0.36]; return <group>{ys.map((y, i) => <SidingStrip key={`ck-h-${i}`} position={[0, y, frontZ]} size={[width, 0.035, 0.02]} color={seam} />)}{frontXs.map((x, i) => <SidingStrip key={`ck-v-${i}`} position={[x, height / 2, frontZ]} size={[0.035, height, 0.02]} color={seam} />)}{ys.flatMap((y, i) => [<SidingStrip key={`ck-lh-${i}`} position={[leftX, y, 0]} size={[0.02, 0.035, depth]} color={seam} />, <SidingStrip key={`ck-rh-${i}`} position={[rightX, y, 0]} size={[0.02, 0.035, depth]} color={seam} />])}{sideZs.flatMap((z, i) => [<SidingStrip key={`ck-lv-${i}`} position={[leftX, height / 2, z]} size={[0.02, height, 0.035]} color={seam} />, <SidingStrip key={`ck-rv-${i}`} position={[rightX, height / 2, z]} size={[0.02, height, 0.035]} color={seam} />])}</group>; }
  if (kind === "fish") { const rows = [0.72, 1.28, 1.84, 2.4, 2.96, 3.52].filter((y) => y < height - 0.16); const frontStarts = [-0.38, -0.08, 0.22]; const sideStarts = [-0.38, -0.08, 0.22]; return <group>{rows.flatMap((y, row) => frontStarts.map((ratio, i) => <SidingStrip key={`fi-f-${row}-${i}`} position={[width * (ratio + (row % 2 ? 0.12 : 0)), y, frontZ]} size={[width * 0.22, 0.045, 0.02]} color={seam} />))}{rows.flatMap((y, row) => sideStarts.flatMap((ratio, i) => [<SidingStrip key={`fi-l-${row}-${i}`} position={[leftX, y, depth * (ratio + (row % 2 ? 0.12 : 0))]} size={[0.02, 0.045, depth * 0.22]} color={seam} />, <SidingStrip key={`fi-r-${row}-${i}`} position={[rightX, y, depth * (ratio + (row % 2 ? 0.12 : 0))]} size={[0.02, 0.045, depth * 0.22]} color={seam} />]))}</group>; }

  const rows = Array.from({ length: Math.max(4, Math.floor(height / 0.52)) }, (_, i) => 0.42 + i * 0.52).filter((y) => y < height - 0.15);
  return <group>{rows.map((y, row) => <group key={`la-${row}`}><SidingStrip position={[0, y, frontZ]} size={[width, 0.035, 0.02]} color={seam} />{Array.from({ length: 4 }, (_, i) => -width / 2 + (i + 1) * (width / 5) + (row % 2 ? width / 10 : 0)).filter((x) => x < width / 2 - 0.12).map((x, i) => <SidingStrip key={`lav-${row}-${i}`} position={[x, y - 0.26, frontZ]} size={[0.035, 0.48, 0.02]} color={seam} />)}<SidingStrip position={[leftX, y, 0]} size={[0.02, 0.035, depth]} color={seam} /><SidingStrip position={[rightX, y, 0]} size={[0.02, 0.035, depth]} color={seam} /></group>)}</group>;
}

function Window({ x, y, z, width = 1.15, bars = false }: { x: number; y: number; z: number; width?: number; bars?: boolean }) {
  return <group position={[x, y, z]}><mesh><boxGeometry args={[width, 1.15, 0.08]} /><meshStandardMaterial color="#8fb5c7" emissive="#6b91a3" emissiveIntensity={0.16} /></mesh><mesh position={[0, 0, 0.055]}><boxGeometry args={[0.055, 1.15, 0.035]} /><meshStandardMaterial color="#ddd6ca" /></mesh><mesh position={[0, 0, 0.055]}><boxGeometry args={[width, 0.055, 0.035]} /><meshStandardMaterial color="#ddd6ca" /></mesh>{bars ? [-0.35, 0, 0.35].map((bx) => <mesh key={bx} position={[bx * width, 0, 0.09]}><boxGeometry args={[0.045, 1.15, 0.04]} /><meshStandardMaterial color="#27272a" /></mesh>) : null}</group>;
}

function SpecialFeature({ feature, width, height, frontZ }: { feature?: Profile["feature"]; width: number; height: number; frontZ: number }) {
  if (feature === "medical") return <group position={[width * 0.33, height * 0.72, frontZ + 0.08]}><mesh><boxGeometry args={[0.24, 0.95, 0.08]} /><meshStandardMaterial color="#dc2626" /></mesh><mesh><boxGeometry args={[0.95, 0.24, 0.08]} /><meshStandardMaterial color="#dc2626" /></mesh></group>;
  if (feature === "townhall" || feature === "bank") return <>{[-width * 0.34, width * 0.34].map((x) => <mesh key={x} position={[x, height * 0.43, frontZ + 0.18]}><cylinderGeometry args={[0.18, 0.22, height * 0.72, 12]} /><meshStandardMaterial color="#e7dfd2" /></mesh>)}</>;
  return null;
}

function MotelUnit({ x, frontZ, number, office = false }: { x: number; frontZ: number; number: number; office?: boolean }) {
  return <group position={[x, 1.15, frontZ]}><mesh><boxGeometry args={[0.9, 2.15, 0.12]} /><meshStandardMaterial color="#6b2f33" /></mesh><mesh position={[0.28, 0, 0.085]}><sphereGeometry args={[0.05, 10, 10]} /><meshStandardMaterial color="#d6b36a" /></mesh><mesh position={[-0.67, 0.08, 0.015]}><boxGeometry args={[0.62, 1.08, 0.09]} /><meshStandardMaterial color="#8fb5c7" emissive="#6b91a3" emissiveIntensity={0.12} /></mesh><Text position={[0, 0.38, 0.09]} fontSize={office ? 0.18 : 0.22} maxWidth={1.7} lineHeight={1.02} textAlign="center" anchorX="center" anchorY="middle" color="#fde68a">{office ? `${number}\nOFFICE` : `${number}`}</Text></group>;
}

function LoogansMotel({ building, onPointerDown, onPointerUp }: { building: BuildingData; onPointerDown: Handler; onPointerUp: Handler }) {
  const [x, , z] = building.position;
  const faceNorth = rowFacesNorth(z);
  const width = 12;
  const height = 3.9;
  const depth = 4.8;
  const frontZ = depth / 2 + 0.065;
  const sidingZ = depth / 2 + 0.018;
  const sidingSeam = darken("#b65f62");
  const clapboardRows = Array.from({ length: 7 }, (_, i) => 0.46 + i * 0.52).filter((y) => y < height - 0.15);
  const unitXs = [-5, -3, -1, 1, 3, 5];
  const parkingLineXs = [7.2, 8.6, 10, 11.4, 12.8];
  return <group position={[x, 0, z]} onPointerDown={onPointerDown} onPointerUp={onPointerUp}><group rotation={[0, faceNorth ? Math.PI : 0, 0]}><mesh position={[0, 0.08, 0]}><boxGeometry args={[width + 0.7, 0.16, depth + 1.3]} /><meshStandardMaterial color="#262626" /></mesh><mesh position={[0, height / 2, 0]}><boxGeometry args={[width, height, depth]} /><meshStandardMaterial color="#b65f62" /></mesh>{clapboardRows.flatMap((y, i) => [<mesh key={`motel-front-${i}`} position={[0, y, sidingZ]} raycast={() => null}><boxGeometry args={[width, 0.05, 0.02]} /><meshStandardMaterial color={sidingSeam} /></mesh>, <mesh key={`motel-back-${i}`} position={[0, y, -sidingZ]} raycast={() => null}><boxGeometry args={[width, 0.05, 0.02]} /><meshStandardMaterial color={sidingSeam} /></mesh>])}<mesh position={[0, height + 0.1, 0]}><boxGeometry args={[width + 0.32, 0.2, depth + 0.32]} /><meshStandardMaterial color="#111111" /></mesh><mesh position={[10, 0.045, 0.25]}><boxGeometry args={[6.2, 0.08, 5.8]} /><meshStandardMaterial color="#35383c" roughness={0.95} /></mesh>{parkingLineXs.map((lineX) => <mesh key={lineX} position={[lineX, 0.095, 0.2]}><boxGeometry args={[0.06, 0.025, 4.7]} /><meshStandardMaterial color="#d9d6c8" /></mesh>)}<mesh position={[-2.25, height - 0.44, frontZ + 0.04]}><boxGeometry args={[4.8, 0.58, 0.09]} /><meshStandardMaterial color="#252525" /></mesh><Text position={[-2.25, height - 0.44, frontZ + 0.1]} fontSize={0.32} maxWidth={4.3} textAlign="center" anchorX="center" anchorY="middle" color="#fde68a">LOOGANS MOTEL</Text><mesh position={[-2.25, height - 0.44, -frontZ - 0.04]} raycast={() => null}><boxGeometry args={[4.8, 0.5, 0.07]} /><meshStandardMaterial color="#252525" /></mesh><Text position={[-2.25, height - 0.44, -frontZ - 0.09]} rotation={[0, Math.PI, 0]} fontSize={0.28} maxWidth={4.3} textAlign="center" anchorX="center" anchorY="middle" color="#fde68a" raycast={() => null}>LOOGANS MOTEL</Text>{unitXs.map((unitX, index) => <MotelUnit key={index} x={unitX} frontZ={frontZ} number={index + 1} office={index === 5} />)}</group></group>;
}

export default function TownBusinessBuilding({ building, onPointerDown, onPointerUp }: { building: BuildingData; onPointerDown: Handler; onPointerUp: Handler; }) {
  const [x, , z] = building.position;
  const [width, height, depth] = building.size;
  const profile = PROFILES[building.id] ?? { wall: building.color, trim: "#d6d3d1", door: "#3f3f46", sign: building.name };
  const frontZ = depth / 2 + 0.055;
  const doorX = width > 4.5 ? width * 0.28 : width * 0.25;
  const windowX = -width * 0.22;
  const secondWindowX = width > 4.2 ? -width * 0.38 : -width * 0.2;
  const barred = profile.feature === "bail" || profile.feature === "police";
  const faceNorth = rowFacesNorth(z);
  const underConstruction = UNDER_CONSTRUCTION_IDS.has(building.id);
  const sidingKind = SIDING_KINDS[building.id];
  const frontSignY = height * 0.82 + (profile.feature === "diner" ? 0.35 : 0);

  if (profile.feature === "motel") return <LoogansMotel building={building} onPointerDown={onPointerDown} onPointerUp={onPointerUp} />;

  return <group position={[x, 0, z]} onPointerDown={onPointerDown} onPointerUp={onPointerUp}><mesh position={[0, height / 2, 0]}><boxGeometry args={[width, height, depth]} /><meshStandardMaterial color={profile.wall} /></mesh><mesh position={[0, height + 0.09, 0]}><boxGeometry args={[width + 0.28, 0.18, depth + 0.28]} /><meshStandardMaterial color="#111111" /></mesh>{profile.feature === "police" ? <><mesh position={[-0.42, height + 0.18, 0]}><boxGeometry args={[0.55, 0.18, 0.35]} /><meshStandardMaterial color="#2563eb" emissive="#1d4ed8" emissiveIntensity={0.6} /></mesh><mesh position={[0.42, height + 0.18, 0]}><boxGeometry args={[0.55, 0.18, 0.35]} /><meshStandardMaterial color="#dc2626" emissive="#b91c1c" emissiveIntensity={0.6} /></mesh></> : null}<group rotation={[0, faceNorth ? Math.PI : 0, 0]}>{sidingKind ? <BusinessSiding width={width} height={height} depth={depth} wall={profile.wall} kind={sidingKind} /> : null}<mesh position={[doorX, 1.15, frontZ]}><boxGeometry args={[0.92, 2.3, 0.1]} /><meshStandardMaterial color={profile.door} /></mesh><mesh position={[doorX + 0.28, 1.15, frontZ + 0.065]}><sphereGeometry args={[0.055, 10, 10]} /><meshStandardMaterial color="#d6b36a" /></mesh>{profile.feature !== "diner" ? <Window x={windowX} y={height * 0.48} z={frontZ} width={Math.min(1.35, width * 0.3)} bars={barred} /> : null}{profile.feature !== "diner" && width >= 5 ? <Window x={secondWindowX} y={height * 0.48} z={frontZ} width={1.05} bars={barred} /> : null}<mesh position={[0, frontSignY, frontZ + 0.04]}><boxGeometry args={[Math.min(width * 0.9, 5.5), 0.72, 0.09]} /><meshStandardMaterial color="#252525" /></mesh><Text position={[0, frontSignY, frontZ + 0.1]} fontSize={Math.min(0.32, width / Math.max(profile.sign.length, 12) * 0.95)} maxWidth={Math.min(width * 0.82, 5.1)} textAlign="center" anchorX="center" anchorY="middle" color={profile.signColor ?? "#f8fafc"}>{profile.sign}</Text><mesh position={[0, height * 0.82, -frontZ - 0.04]} raycast={() => null}><boxGeometry args={[Math.min(width * 0.82, 5.0), 0.58, 0.07]} /><meshStandardMaterial color="#252525" /></mesh><Text position={[0, height * 0.82, -frontZ - 0.09]} rotation={[0, Math.PI, 0]} fontSize={Math.min(0.27, width / Math.max(profile.sign.length, 12) * 0.82)} maxWidth={Math.min(width * 0.74, 4.7)} textAlign="center" anchorX="center" anchorY="middle" color={profile.signColor ?? "#f8fafc"} raycast={() => null}>{profile.sign}</Text>{underConstruction ? <group position={[0, height * 0.34, frontZ + 0.12]}><mesh><boxGeometry args={[2.25, 0.72, 0.05]} /><meshStandardMaterial color="#f5e7c8" /></mesh><Text position={[0, 0, 0.035]} fontSize={0.16} maxWidth={2.0} lineHeight={1.12} textAlign="center" anchorX="center" anchorY="middle" color="#1f2937">{"UNDER CONSTRUCTION\ncheck back soon"}</Text></group> : null}{profile.awning ? <mesh position={[0, profile.feature === "diner" ? 2.48 : height * 0.63, frontZ + 0.38]} rotation={[0.18, 0, 0]} raycast={() => null}><boxGeometry args={[width * 0.9, 0.12, 0.8]} /><meshStandardMaterial color={profile.awning} /></mesh> : null}{profile.feature === "realty" ? <group position={[-width * 0.24, height * 0.42, frontZ + 0.11]}>{[-0.35, 0, 0.35].map((dx, index) => <mesh key={dx} position={[dx, index % 2 ? -0.22 : 0.2, 0]}><boxGeometry args={[0.28, 0.34, 0.035]} /><meshStandardMaterial color="#f5f5dc" /></mesh>)}</group> : null}{profile.feature === "diner" ? <mesh position={[-width * 0.28, 0.92, frontZ + 0.07]}><boxGeometry args={[width * 0.42, 1.35, 0.08]} /><meshStandardMaterial color="#a8d4de" emissive="#79aebb" emissiveIntensity={0.12} /></mesh> : null}<SpecialFeature feature={profile.feature} width={width} height={height} frontZ={frontZ} /></group></group>;
}
